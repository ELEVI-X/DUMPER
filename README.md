# Command
```
pkg update && pkg upgrade
pkg install python && pkg install git
rm -rf DUMPER
git clone --depth 1 https://github.com/ELEVI-X/DUMPER
cd DUMPER
pip3 install -r requirements.txt
python3 run.py
```
